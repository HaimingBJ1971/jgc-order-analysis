# 金谷仓餐饮订单与桌访合并分析系统 — PRD

> 版本 3.17 | 2026-06-24

## 一、项目概述

本项目为金谷仓餐厅（万荷店、保利店、湾里店）的日常经营数据分析系统，包含以下核心工具：

1. **订单+桌访合并分析**：将 POS 散单合并为消费团体，关联桌访反馈修正人数，重新计算人均消费，输出 PDF/MD 报告
2. **长期订单分析**：跨年度多文件加载，每日统计汇总为 4-Sheet Excel，SQLite 数据库持久化增量更新
3. **周期对比分析**：以周或月为周期，环比（上周/上月）和同比（去年同周/同月）经营数据对比，输出 PDF/Word 报告
4. **平台外卖统计**：平台外卖订单多维统计，输出 Excel + PDF + MD，可选写入 SQLite
5. **饮品/酒水订单统计**（万荷按需）：按商品中类统计饮品酒水团体数、销量、点购率与收入；支持周/月同比环比（本期 Excel + 主库对比期）；含对比时 A4 横置 PDF
6. **桌访语料转换**：桌访 CSV 转为四维度心理学奖所需的「语料桌访」xlsx

**核心价值**：POS 散单合并后的人均消费（客单价）才是真实的堂食经营指标；平台外卖单独统计，避免与堂食客单价混淆。长期数据积累和周期对比帮助管理者发现趋势与异常。GUI 降低命令行使用门槛，通过运行前预校验减少文件选错、字段缺失、日期不完整导致的返工。

## 二、项目目录结构

```
订单与桌访合并/
│
├── PRD.md                          # ← 本文档
│
├── GUI控制台/                       # ★ 统一图形入口：拖拽文件、预校验、运行四个业务功能
│   ├── main.py                      #    GUI 启动入口
│   ├── app_state.py                 #    最近目录、数据库路径、门店偏好等本地状态
│   ├── validators.py                #    文件识别与预校验规则
│   ├── task_runner.py               #    后台任务调度，调用现有 CLI/模块入口
│   ├── output_registry.py           #    输出文件登记、打开报告/打开目录
│   ├── views/                       #    四个业务功能页面
│   │   ├── order_zhuofang_view.py   #    订单+桌访合并/纯订单分析
│   │   ├── long_term_view.py        #    长期订单分析
│   │   ├── period_compare_view.py   #    周期对比分析
│   │   └── takeaway_view.py         #    平台外卖统计
│   └── requirements.txt             #    PySide6 + 现有分析依赖
│
├── 订单桌访合并/                    # ★ 主工具：订单+桌访合并分析
│   ├── merge_order_zhuofang.py     #    CLI 入口，完整 pipeline
│   ├── requirements.txt            #    pandas, openpyxl, reportlab
│   ├── README.md                   #    工具使用说明
│   ├── output/                     #    输出目录（PDF + MD）
│   ├── 店内订单明细_*.xlsx          #    输入：POS 订单 Excel
│   └── 桌探数据_*.csv              #    输入：桌访反馈 CSV
│
├── 每日订单分析/                    # 核心算法库 + 历史文档
│   ├── order_merger_skill/         # ★ 核心 Python 包
│   │   ├── __init__.py             #    包标识，版本 1.0.0
│   │   ├── config.py               #    所有可调参数
│   │   ├── data_loader.py          #    Excel 加载与清洗
│   │   ├── order_merger.py         #    核心合并算法
│   │   ├── aggregator.py           #    聚合、过滤、统计
│   │   ├── item_report_helpers.py  #    报告用商品展示辅助
│   │   ├── report_generator.py     #    Markdown 报告生成
│   │   ├── pdf_generator.py        #    PDF「客单价重点订单分析」
│   │   ├── pdf_generator_complete.py #  PDF「全量订单列表」
│   │   └── README.md               #    包使用说明
│   │
│   ├── PRD-*.md                    #    历史 PRD 文档（仅供参考）
│   ├── 订单合并逻辑优化_*.plan.md    #    合并算法优化设计文档
│   └── *.md                        #    历史输出报告
│
├── 长期订单分析/                    # ★ 跨日期长期订单趋势分析
│   ├── main.py                     #    CLI 入口，完整 pipeline
│   ├── db_manager.py               #    SQLite 读写、增量检测
│   ├── multi_file_loader.py        #    多文件加载与去重
│   ├── daily_stats.py              #    每日统计计算（4个Sheet）
│   ├── excel_writer.py             #    4-Sheet Excel 输出
│   └── requirements.txt            #    pandas, openpyxl
│
├── 周期对比分析/                    # ★ 周期环比/同比对比分析
│   ├── main.py                     #    CLI 入口，完整 pipeline
│   ├── period_validator.py         #    周期完整性校验
│   ├── comparator.py               #    同比环比计算引擎
│   ├── pdf_report.py               #    PDF 对比报告生成
│   ├── word_report.py              #    Word 对比报告生成
│   └── requirements.txt            #    pandas, openpyxl, reportlab, python-docx
│
├── 平台外卖统计/                    # ★ 平台外卖订单统计分析
│   ├── main.py
│   └── ...
├── 饮品订单统计/                    # ★ 万荷饮品/酒水中类占比 PDF（手工按需）
│   ├── generate_drink_order_stats_pdf.py
│   └── README.md
└── 桌访语料转换/                    # ★ 桌访 CSV → 语料桌访 xlsx
    ├── convert_corpus.py
    └── README.md
```

## 三、快速开始

### 3.1 环境要求

- Python 3.9+
- macOS / Linux（Windows 需自行调整中文字体路径）

### 3.2 安装

```bash
# 克隆或进入项目目录
cd 订单与桌访合并

# 安装依赖（只需执行一次；如只使用 CLI，可暂不安装 PySide6）
pip install pandas openpyxl reportlab python-docx PySide6
```

### 3.3 启动：统一 GUI 操作台（推荐入口）

```bash
cd GUI控制台

python3 main.py
```

GUI 操作台提供四个功能页：

| 功能页 | 数据文件 | 输出 |
|------|----------|------|
| 订单+桌访合并 | POS 订单 Excel；可选桌访 CSV | PDF + Markdown；不提供 CSV 时为纯订单分析 |
| 长期订单分析 | 一个或多个 POS 订单 Excel；SQLite 数据库路径 | 4-Sheet Excel + SQLite 增量更新 |
| 周期对比分析 | 当前周期 POS 订单 Excel；SQLite 数据库路径；周/月模式 | PDF + Word，并写入当前周期数据 |
| 平台外卖统计 | 一个或多个平台外卖订单 Excel；可选 SQLite 数据库路径 | 8-Sheet Excel + PDF + Markdown，可写入外卖统计表 |

GUI 必须支持文件/文件夹拖入、自动识别文件类型、运行前预校验、错误/警告分级展示、后台运行进度、输出文件列表、打开报告和打开输出目录。

CLI 入口继续保留，便于开发测试、自动化运行和异常排查。

### 3.4 启动：订单+桌访合并分析（CLI）

```bash
cd 订单桌访合并

python3 merge_order_zhuofang.py \
    --excel "店内订单明细2026-04-29+00_00_00~2026-04-29+23_59_59.xlsx" \
    --csv "桌探数据_1.5版_50条_2026-4-29.csv"
```

**参数说明：**

| 参数 | 必填 | 说明 |
|------|------|------|
| `--excel` | 是 | POS 订单 Excel 文件路径（.xlsx） |
| `--csv` | 否 | 桌访反馈 CSV 文件路径；不传则进入纯订单分析模式，不关联桌访 |
| `--store` | 否 | 门店名称（万荷店/保利店/湾里店），不传则自动推断 |
| `--output-dir` | 否 | 输出目录，默认为 Excel 所在目录下的 `output/` 子目录 |

**输出物**（位于 `output/` 目录）：

- `订单桌访合并_YYYYMMDD_门店.pdf` — 纵向 A3 PDF 完整报告（跨天时为 `_起始日期_结束日期_`）
- `订单桌访合并_YYYYMMDD_门店.md` — Markdown 摘要

### 3.5 启动：长期订单分析（CLI）

```bash
cd 长期订单分析

python3 main.py \
    --files "路径/文件1.xlsx" "路径/文件2.xlsx" \
    --db "output/长期订单分析.db" \
    --output-dir ./output
```

**参数说明：**

| 参数 | 必填 | 说明 |
|------|------|------|
| `--files` | 是 | 一个或多个 POS 订单 Excel 文件 |
| `--db` | 是 | SQLite 数据库路径（不存在则新建，自动增量更新） |
| `--output-dir` | 否 | 输出目录，默认当前目录 |

**输出物**：
- `长期订单分析_YYYYMMDD_YYYYMMDD.xlsx` — 4-Sheet Excel（数据总览、订单数量明细、客单价区间分布、开单人统计）
- SQLite 数据库 — 持久化全量数据，支持增量更新

### 3.6 启动：周期对比分析（CLI）

```bash
cd 周期对比分析

python3 main.py \
    --excel "店内订单明细_2026-05-11+...xlsx" \
    --db "../长期订单分析/output/长期订单分析.db" \
    --mode week \
    --store "万荷店" \
    --output-dir ./output
```

**参数说明：**

| 参数 | 必填 | 说明 |
|------|------|------|
| `--excel` | 是 | 当前周期的 POS 订单 Excel 文件 |
| `--db` | 是 | 长期订单分析 SQLite 数据库路径 |
| `--mode` | 是 | `week`（周一~周日）或 `month`（1号~月底） |
| `--store` | 否 | 门店名称，不传则自动推断 |
| `--output-dir` | 否 | 输出目录，默认 `./output` |

**输出物**：
- `周期对比分析_YYYYMMDD_YYYYMMDD_门店.pdf` — PDF 对比报告
- `周期对比分析_YYYYMMDD_YYYYMMDD_门店.docx` — Word 对比报告
- 当前周期数据自动写入 SQLite 数据库

报告含五个章节：一、经营数据对比；二、酒水饮料甜品销售排行；三、重点菜品对比；四、商品中类销售额分布；五、客单价区间对比。每项含本期值、环比变化及变化率、同比变化及变化率，绿色为增长、红色为下降。

### 3.7 启动：平台外卖统计（CLI）

```bash
cd 平台外卖统计

python3 main.py \
    --files "../原始数据/万荷平台外卖订单明细2026-05-18+00_00_00~2026-05-24+23_59_59.xlsx" \
            "../原始数据/保利平台外卖订单明细2026-05-18+00_00_00~2026-05-24+23_59_59.xlsx" \
    --db "../长期订单分析/output/长期订单分析.db" \
    --output-dir ./output
```

**参数说明：**

| 参数 | 必填 | 说明 |
|------|------|------|
| `--files` | 是 | 一个或多个平台外卖订单明细 Excel 文件 |
| `--db` | 否 | SQLite 数据库路径；传入时写入外卖统计相关表，用于长期趋势 |
| `--store` | 否 | 门店名称；不传则从文件名或 Excel 顶部"门店名称"自动推断 |
| `--output-dir` | 否 | 输出目录，默认 `./output` |

**输出物**：
- `平台外卖统计_YYYYMMDD_YYYYMMDD.xlsx` — 多 Sheet Excel（数据总览、门店对比、每日趋势、平台来源、时段分布、履约效率、退单异常、脱敏明细）
- `平台外卖统计_YYYYMMDD_YYYYMMDD.pdf` — PDF 摘要报告
- `平台外卖统计_YYYYMMDD_YYYYMMDD.md` — Markdown 摘要
- 可选 SQLite 写入 — 追加外卖订单明细与聚合统计表

### 3.8 启动：饮品/酒水订单统计（CLI，万荷按需）

```bash
cd 饮品订单统计

python3 generate_drink_order_stats_pdf.py \
    --excel "../path/万荷店内订单明细2026-06-15+....xlsx" \
    --output "./万荷饮品酒水订单统计_20260615_20260621.pdf" \
    --store "万荷餐厅" \
    --mode week \
    --db "../长期订单分析/output/长期订单分析.db"
```

**参数说明：**

| 参数 | 必填 | 说明 |
|------|------|------|
| `--excel` | 是 | 万荷店内订单明细 Excel（本期） |
| `--output` | 是 | 输出 PDF 路径 |
| `--store` | 否 | 报告标题门店名 |
| `--mode` | 否 | `week` 或 `month`；启用同比环比 |
| `--db` | 否 | SQLite 主库（`--mode` 时默认项目主库） |
| `--start` / `--end` | 否 | 无 `--mode` 时周期展示；有 `--mode` 时从 Excel 校验 |

**统计口径**：

- 复用 `merge_order_zhuofang.load_and_process_orders()`：堂食、去外点自取、同桌补单合并、过滤免单/零食/打包/零散/吧台
- **团体数 / 就餐人数** 与订单桌访合并报告一致，不使用 POS 原始单数
- 十一类商品中类：非酒精（调饮汁、饮料和水果、茶、咖啡、冰淇淋）→ 含酒精（啤酒、白酒、葡萄酒、鸡尾酒、苏格兰威士忌、黄酒）
- 饮品收入采用 POS 店内商品归因口径：排除套餐父项，保留有收入的套餐子项；额占比分母为整体营业额（POS 店内订单收入 + 第三方平台外卖已完成订单收入）
- 点购率 = 销量 ÷ 统计范围就餐人数合计（一人一杯 = 100%）
- 环比/同比期来自主库 `groups`（`filter_status=kept`），周期规则同「周期对比分析」

**输出物**（详见 `饮品订单统计/README.md`）：

| 模式 | 版式 | 结构 |
|------|------|------|
| `--mode week\|month` | A4 横置 | 摘要三期原始数据 + 【本周说明】正文；一/三为本周 9 列简表；二/四为分品类与维度同比环比三行对比表（本期=100%） |
| 无 `--mode` | A4 纵置 | 摘要 + 分品类表 + 维度合并表 + 口径说明 |

**周期属性**：手工按需；**未纳入** `run_cycle.py` 自动 pipeline。

### 3.9 启动：桌访语料转换（CLI）

```bash
cd 桌访语料转换

python3 convert_corpus.py \
    "桌访数据_2026-06-08_至_2026-06-14_467条.csv"
```

**输出物**：`语料桌访_1.5版_N条_YYYY-M-D.xlsx` — 供四维度「心理学案例采纳奖」语料输入。详见 `桌访语料转换/README.md`。

### 3.10 启动：桌访数据报告（独立参考工具，非本仓库维护）

```bash
cd 桌访技能工作目录

python3 .trae/skills/zhuofang-report-generator/zhuofang_processor.py \
    桌探数据_1.5版_50条_2026-4-29.csv \
    --dir output
```

**输出物**：
- `桌访报告_YYYYMMDD.xlsx` — 格式化的桌访数据 Excel 表
- `桌访报告_YYYYMMDD.pdf` — 横向桌访数据 PDF
- `桌访分析原始记录_YYYYMMDD.md` — 供 AI 消费的分析源数据 Markdown

## 四、输入数据格式

### 4.1 POS 订单 Excel

文件名格式：`店内订单明细YYYY-MM-DD+...xlsx`

必须包含两个 Sheet：

**Sheet 1：「店内订单明细」**（订单主表）

| 字段 | 类型 | 说明 |
|------|------|------|
| 订单号 | str | 纯数字，唯一标识 |
| 下单时间 | datetime | |
| 结账时间 | datetime | |
| 订单金额 | float | 原始金额 |
| 订单收入 | float | 实收金额（折后） |
| 就餐人数 | int | POS 登记的就餐人数 |
| 订单类型 | str | 必须为"堂食"才会纳入统计 |
| 桌台 | str | 含"外点自取"的会被排除 |
| 会员手机号 | str | 用于合并弱规则 |
| 支付方式 | str | 用于合并弱规则 |
| 整单备注 | str | |

**Sheet 2：「商品-店内订单明细」**（商品明细）

| 字段 | 类型 | 说明 |
|------|------|------|
| 订单号 | str | 关联订单主表 |
| 商品名称 | str | |
| 商品中类 | str | 商品分类（如"对外售卖零食"） |
| 规格 | str | |
| 单价 | float | |
| 数量 | float | |
| 菜品合计金额 | float | |

### 4.2 桌访 CSV

文件名格式：`桌探数据_1.5版_N条_YYYY-M-D.csv`

核心字段：

| 字段 | 说明 |
|------|------|
| 订单号 | 用于与 POS 订单关联 |
| 就餐人数 | 服务员桌访时确认的实际人数（比 POS 更可靠） |
| 店面 | 万荷店 / 保利店 / 湾里店 |
| 服务员 | 桌访执行人 |
| 语音转录 | 桌访对话文本 |
| 总体满意度评分 | |
| 创建时间 | |

### 4.3 平台外卖订单 Excel

文件名建议格式：`门店平台外卖订单明细YYYY-MM-DD+...xlsx`，例如 `万荷平台外卖订单明细2026-05-18+00_00_00~2026-05-24+23_59_59.xlsx`。

必须包含 Sheet：「平台外卖订单明细」。该 Sheet 顶部有报表标题与元信息，真实表头行需通过关键字段 `外卖订单号` 自动定位；示例文件中表头位于第 6 行。末尾可能存在 `合计` 行，必须剔除。

**顶部元信息：**

| 字段 | 说明 |
|------|------|
| 门店名称 | 用于推断门店，如 `金谷仓家庭料理万荷餐厅`、`金谷仓（保利广场）` |
| 下单时间 | 报表导出时间范围 |
| 订单来源 | 报表筛选条件，通常为"全部" |
| 制表时间 | 导出时间 |
| 制表人 | 导出人 |

**订单明细字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| 订单来源 | str | 平台来源，如美团外卖、淘宝闪购餐饮外卖 |
| 外卖流水号 | str | 平台流水号 |
| 外卖订单号 | str | 平台订单唯一标识，去重主键之一 |
| 收银订单号 | str | POS 收银订单号，仅用于追溯，不与堂食订单合并 |
| 下单时间 | datetime | 用于时段统计 |
| 营业日 | date | 默认统计日期 |
| 接单时间 | datetime | 用于接单耗时 |
| 送达时间 | datetime | 用于履约耗时 |
| 订单状态 | str | `已完成` 计入有效外卖；`已退单` 等单独统计，不计入有效营业 |
| 配送状态 | str | 如已送达、待配送 |
| 配送方式 | str | 如美团配送、淘宝闪购配送 |
| 订单金额 | float | 平台原始订单金额 |
| 菜品合计金额 | float | 菜品原价合计 |
| 附加费分摊 | float | 平台附加费分摊 |
| 菜品优惠 | float | 菜品层面优惠，通常为负数 |
| 菜品收入 | float | 平台计算的菜品收入 |
| 餐盒费 | float | 餐盒收入 |
| 打包费 | float | 打包费 |
| 配送费 | float | 顾客或平台配送费字段 |
| 订单优惠 | float | 商家/订单层面优惠，通常为负数 |
| 平台优惠 | float | 平台补贴优惠，通常为负数 |
| 顾客实付 | float | 顾客实际支付金额 |
| 订单支出 | float | 平台费用合计，通常为负数 |
| 外卖抽佣 | float | 平台佣金，通常为负数 |
| 配送支出 | float | 配送费用支出，通常为负数 |
| 其他支出 | float | 其他费用支出，通常为负数 |
| 部分退款 | float | 部分退款金额 |
| 订单收入 | float | 平台订单收入统计口径，外卖统计默认以此作为外卖营业额 |
| 首次对账时间 | datetime | 平台对账时间 |

**隐私字段处理：** `收货人姓名`、`收货人手机号`、`送餐地址` 仅允许用于必要的内部排查；默认不得输出到 PDF/MD/Excel 汇总页，不得写入长期统计数据库。若输出脱敏明细，只保留字段占位或脱敏值。

### 4.4 GUI 文件拖入与预校验

GUI 操作台需支持拖入单个文件、多个文件或文件夹。拖入文件夹时递归深度默认 1 层，只扫描 `.xlsx`、`.xls`、`.csv`，忽略临时锁文件（如 `~$*.xlsx`）和输出目录中的历史报告文件。

**文件类型自动识别：**

| 识别结果 | 判定规则 |
|----------|----------|
| POS 订单 Excel | 工作簿同时包含「店内订单明细」与「商品-店内订单明细」，或文件名包含 `店内订单明细` |
| 桌访 CSV | 文件名包含 `桌探数据` 或 CSV 表头包含 `订单号`、`就餐人数`、`语音转录`、`总体满意度评分` 等桌访字段 |
| 平台外卖订单 Excel | 工作簿包含「平台外卖订单明细」，或可在前 20 行定位到 `外卖订单号` 表头 |
| SQLite 数据库 | 扩展名为 `.db` 或 `.sqlite`，且可读取；用于长期/周期/外卖增量数据 |
| 未识别文件 | 不进入运行队列，在预校验结果中列为警告 |

**预校验分级：**

| 级别 | 含义 | 是否允许运行 |
|------|------|--------------|
| Error | 缺少必需文件、Sheet、关键字段，数据库不可读/不可写，日期无法解析等会导致任务失败的问题 | 不允许运行 |
| Warning | 门店无法自动确认、日期范围与文件名不一致、跨门店文件混用、历史数据不足、存在退单/空金额等需要人工注意的问题 | 允许运行，但需在界面醒目展示 |
| Info | 识别出的门店、日期范围、订单数、桌访记录数、外卖平台来源、预计输出目录等运行摘要 | 允许运行 |

**各功能页最低预校验要求：**

| 功能页 | 必需文件 | 必检项 |
|--------|----------|--------|
| 订单+桌访合并 | 1 个 POS 订单 Excel；桌访 CSV 可选 | POS 两个 Sheet 存在；订单主表关键字段完整；商品明细可关联订单号；桌访 CSV 若提供则编码可读、核心字段存在；无 CSV 时明确提示将执行纯订单分析 |
| 长期订单分析 | 1 个或多个 POS 订单 Excel；1 个 SQLite 路径 | 每个 POS 文件结构合格；可解析营业日期；数据库路径所在目录可写；重复导入时显示预计新增/已存在文件或日期范围提示 |
| 周期对比分析 | 1 个当前周期 POS 订单 Excel；1 个 SQLite 数据库；周/月模式 | 当前文件结构合格；数据库存在且含长期订单分析基础表；按 `week` 或 `month` 校验当前周期完整性；环比/同比数据缺失时列为 Warning |
| 平台外卖统计 | 1 个或多个平台外卖订单 Excel；SQLite 可选 | 能定位外卖真实表头；必需金额/时间/状态字段存在；合计行可剔除；门店可推断或由用户选择；隐私字段不进入输出与长期库 |

GUI 不直接修改原始数据文件；所有清洗、脱敏、过滤均在内存或输出文件中完成。

## 五、数据 Pipeline 流程

```
Excel 文件
  │
  ├─ load_excel()          读取两个 Sheet，自动定位表头行
  ├─ [计数]                 统计清洗前原始订单总数 + 外点自取订单数
  ├─ clean_orders()        清洗：去汇总行、只保留堂食、排除外点自取
  ├─ clean_items()         清洗：去汇总行、数值类型转换
  │
  ├─ merge_orders()        ★ 核心合并算法（见第六章）
  │   └─ 输出 orders_with_group（每笔订单新增"消费团体ID"列）
  │
  ├─ aggregate_groups()    聚合为消费团体，计算人均
  │   ├─ 过滤免单（订单收入 <= 0）
  │   ├─ 团体人数 ← 取最高收入订单的就餐人数
  │   ├─ 人均消费 = 订单收入 / 团体人数
  │   └─ filter_groups()  过滤：人均<=0、纯零食单、纯打包单、零散小单（1人1件<100元）、吧台订单
  │
  ├─ [stats 覆盖]          原始订单数 = 清洗前总数，补入外点自取订单数
  │
  ├─ [CSV 加载]            分离有效记录（有订单号）与未识别记录（无订单号）
  │
  ├─ [订单号清洗]          有效记录中订单号在 POS 中不存在的 → 转入三选二匹配队列
  │                       （解决桌访订单号与 POS 格式不一致导致的静默丢弃）
  │
  ├─ match_unrecognized_   对无订单号 / 订单号不匹配的记录，通过桌台号 + 支付金额 + 时间
  │   by_table_amount()    「三选二」模糊匹配（见 5.2 节）
  │
  ├─ associate_feedback()  关联桌访 CSV
  │   ├─ 按订单号匹配桌访记录（含三选二匹配补入的记录）
  │   ├─ 三选二匹配的记录标注 _amount_matched=True
  │   ├─ 如果桌访人数 ≠ POS人数 → 以桌访为准，标记"人数修正"
  │   └─ 重新计算该团体的人均消费
  │
  ├─ 修正 stats 总人数     如果发生人数修正，更新总人数和整体人均
  │
  ├─ detect_anomalies()    异常检测（见第七章）
  │
  ├─ generate_pdf_report() 生成 PDF（见第八章）
  └─ generate_markdown_report() 生成 Markdown
```

### 5.2 桌访匹配策略（三级递进）

桌访 CSV 中可能存在订单号缺失的记录。系统采用三级递进策略，确保每条桌访都有落着：

**第一级：订单号精确匹配**
- `associate_feedback()` 按 CSV 中的 `订单号` 字段与 POS 订单的 `订单号` 精确匹配
- 命中的记录直接关联；未命中的记录（含订单号不在 POS 中的记录）进入下一级

**第二级：三变量「三选二」模糊匹配**
- `match_unrecognized_by_table_amount()` 对第一级未匹配的记录，通过以下三个变量中任意两个一致来匹配：
  - **桌台号**：子串模糊匹配（CSV 的 "B07" ↔ 订单的 "大厅B区B07"）
  - **支付金额**：与订单的 `订单金额` 或 `订单收入` 相等（精确到元）
  - **下单时间**：CSV 下单时间与订单下单时间相差 ≤ 5 分钟
- 三个变量中任意两个条件满足，即视为同一订单
- 命中则注入订单号、标记 `_amount_matched=True`，并在报告中标注「此桌访订单号缺失，通过桌台号与支付金额一致匹配至本订单」

**第三级：未匹配记录公示**
- 前两级均未命中的记录，在 PDF「订单索引」标题下方以独立表格完整列出
- 包含字段：服务员、创建时间、桌台号、就餐人数、支付金额、下单时间、会员状态、语音转录（全文）
- 供人工核查，确保每条桌访不丢失、不埋没

### 5.3 平台外卖统计 Pipeline

平台外卖统计不参与堂食订单合并，也不进入堂食客单价、人均消费、桌访覆盖率等指标。外卖统计是独立经营维度。

```
平台外卖 Excel 文件
  │
  ├─ load_takeaway_excel()   读取「平台外卖订单明细」，通过"外卖订单号"定位真实表头
  ├─ [元信息解析]             读取门店名称、导出时间范围、制表时间
  ├─ clean_takeaway_orders() 剔除标题行、空行、合计行；标准化日期/时间/金额字段
  │   ├─ 去重键：门店 + 订单来源 + 外卖订单号
  │   ├─ 隐私字段：收货人姓名/手机号/地址默认丢弃或脱敏
  │   └─ 门店推断：优先 CLI --store，其次顶部门店名称，其次文件名
  │
  ├─ split_status()          区分有效外卖、退单/取消单、异常状态
  │   ├─ 有效外卖：外卖订单号为数字且订单状态 = 已完成
  │   ├─ 退单/取消：订单状态 != 已完成，单独统计，不计入有效营业额
  │   └─ 合计行：序号 = 合计 或 外卖订单号 = "-"，必须剔除
  │
  ├─ compute_takeaway_stats()
  │   ├─ 总览：订单数、订单金额、订单收入、顾客实付、优惠、抽佣、订单支出、退款
  │   ├─ 门店对比：万荷店/保利店/湾里店横向对比
  │   ├─ 每日趋势：按营业日统计订单数、收入、客单价、抽佣率
  │   ├─ 平台来源：美团外卖、淘宝闪购餐饮外卖等平台对比
  │   ├─ 时段分布：按下单小时、午市/晚市统计外卖高峰
  │   ├─ 履约效率：接单耗时、送达耗时、下单到送达总耗时
  │   └─ 退单异常：已退单、待配送、部分退款、金额为 0 等记录
  │
  ├─ write_takeaway_db()     可选写入 SQLite 外卖相关表
  ├─ generate_excel_report() 生成多 Sheet Excel
  ├─ generate_pdf_report()   生成 PDF 摘要报告
  └─ generate_markdown_report() 生成 Markdown 摘要
```

**核心统计口径：**

| 指标 | 公式 / 口径 |
|------|-------------|
| 有效外卖订单数 | `订单状态 == "已完成"` 且 `外卖订单号` 为数字 |
| 退单数 | `订单状态 != "已完成"` 且 `外卖订单号` 为数字 |
| 外卖营业额 | 有效外卖 `订单收入` 合计 |
| 外卖客单价 | 外卖营业额 / 有效外卖订单数 |
| 顾客实付 | 有效外卖 `顾客实付` 合计 |
| 平台抽佣 | 有效外卖 `外卖抽佣` 合计，展示时保留负数，同时可展示绝对值 |
| 抽佣率 | `abs(外卖抽佣) / 顾客实付`，顾客实付为 0 时显示 `-` |
| 订单支出率 | `abs(订单支出) / 顾客实付`，顾客实付为 0 时显示 `-` |
| 优惠合计 | `abs(菜品优惠) + abs(订单优惠) + abs(平台优惠)` |
| 平均送达时长 | `送达时间 - 下单时间`，仅统计时间字段完整且已送达记录 |
| 接单耗时 | `接单时间 - 下单时间`，仅统计时间字段完整记录 |

注：`顾客实付 + 订单支出` 不能默认视为最终到账金额；如需财务结算口径，需经营或财务团队另行确认。

### 5.4 统一 GUI 操作台 Pipeline

GUI 操作台是四个业务功能的编排层，不承载核心统计逻辑。所有业务计算仍由现有 CLI 或可复用模块完成。

```
拖入文件 / 选择文件 / 选择文件夹
  │
  ├─ scan_files()             过滤临时文件、历史输出文件、不可读文件
  ├─ classify_files()         自动识别 POS Excel、桌访 CSV、外卖 Excel、SQLite DB
  ├─ validate_inputs()        按功能页执行 Error / Warning / Info 分级预校验
  │   ├─ 展示识别结果：门店、日期范围、订单数、记录数、平台来源、数据库状态
  │   ├─ Error 阻断运行，要求用户补文件或调整参数
  │   └─ Warning 允许继续，但必须在任务摘要中保留
  │
  ├─ collect_params()         收集门店、输出目录、数据库路径、周期模式等参数
  ├─ build_command()          生成等价 CLI 命令或模块调用参数
  ├─ run_task_background()    后台线程/进程运行，界面保持可响应
  │   ├─ 标准输出/错误日志实时展示
  │   ├─ 禁止同时运行多个会写入同一 SQLite 数据库的任务
  │   └─ 失败时保留错误信息、命令参数、预校验结果
  │
  ├─ collect_outputs()        扫描并登记 PDF/MD/Excel/Word/DB 输出
  └─ result_view()            显示运行状态、输出文件列表、打开报告、打开输出目录
```

**GUI 运行边界：**

- GUI 不重写 `merge_order_zhuofang.py`、长期订单、周期对比、平台外卖统计中的业务算法
- GUI 可优先通过 Python 模块函数调用现有 pipeline；如果现有入口暂不易复用，可先通过 `subprocess` 调 CLI，但参数必须由结构化对象生成，避免拼接命令字符串
- GUI 必须保留“复制等价 CLI 命令”能力，方便开发团队复现问题
- GUI 运行任务时不得吞掉底层报错；错误信息需展示给用户，并写入本次运行日志
- GUI 的预校验只判断结构完整性和明显业务前置条件，不替代最终 pipeline 的业务校验

## 六、核心合并算法

### 6.1 设计目标

同一桌客人可能分多次扫码点单（先点菜、后加酒水、再加甜点），POS 系统会生成多笔独立订单。合并算法将这些散单归并为一个"消费团体"，从而计算真实的人均消费。

### 6.2 合并范围

**仅在相同桌台内合并**，不同桌台之间永不合并。

### 6.3 规则优先级（第一个命中的规则做决策）

```
R0 并发拆单         → 直接合并
R1 时间窗口         → 新开会话（小单例外）
R2 加单金额上限      → 新开会话
R3 结账后间隔        → 新开会话（小单例外）
R3.5 包间晚餐酒水加单 → 直接合并
弱规则打分          → >= 60 合并 / < 60 新开会话
```

### 6.4 规则详情

**R0 — 并发拆单识别**
- 条件：当前订单下单时间距离上一笔订单下单时间 < 5 分钟
- 决策：直接合并（同桌多人同时各自扫码）
- 参数：`CONCURRENT_MINUTES = 5`

**R1 — 时间窗口**
- 条件：当前订单下单时间距离会话首单下单时间超过窗口时长
- 普通桌台：2 小时 / 包间：3 小时
- 决策：新开会话
- 例外：候选订单为小单（收入 ≤ 锚点 × 30% 或 行数 ≤ 2），不因时间窗口拆分
- 参数：`T_WINDOW_HOURS_REGULAR = 2`、`T_WINDOW_HOURS_PRIVATE = 3`

**R2 — 加单金额上限**
- 条件：候选订单收入 > 锚点订单收入 × 50% 且 锚点订单收入 > 20 元
- 含义：新订单金额远大于主单，说明可能翻台了；但首单为低额小单（如¥5纸巾/茶位费等小微开单）时不适用R2拆分。
- 决策：新开会话
- 参数：`ADD_ON_MAX_RATIO = 0.50`、`ANCHOR_MIN_AMOUNT = 20.0`

**R3 — 结账后间隔**
- 条件：距离上一笔订单结账时间超过间隔，且候选订单不是小单
- 普通桌台：30 分钟 / 包间：120 分钟
- 决策：新开会话
- 参数：`T_REOPEN_MIN_REGULAR = 30`、`T_REOPEN_MIN_PRIVATE = 120`

**R3.5 — 包间晚餐纯酒水加单**
- 条件：包间 + 晚餐时段（17:00-23:00）+ 候选订单全部商品为酒水类
- 决策：直接合并（包间客人饭后加酒水是同一拨人）

**弱规则打分**
- 起始基础分 20 分
- 加分项：时间接近 +25/+10、手机号一致 +25、就餐人数相同 +15、支付方式重叠 +5、Jaccard 商品相似度 ≥ 0.1 +10、小单额外加分 +55/+30/+15、备注一致 +5
- 减分项：手机号不一致 -10
- 决策：总分 >= 60 → 合并；< 60 → 新开会话

### 6.5 小单定义

满足任一条件即为小单：
- `候选订单收入 <= 锚点订单收入 × 30%`
- `候选订单商品行数 <= 2`

### 6.6 可调参数完整清单

所有参数集中在 `每日订单分析/order_merger_skill/config.py`：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `T_WINDOW_HOURS_REGULAR` | 2 | 普通桌合并时间窗口（小时） |
| `T_WINDOW_HOURS_PRIVATE` | 3 | 包间合并时间窗口（小时） |
| `CONCURRENT_MINUTES` | 5 | 并发下单识别阈值（分钟） |
| `ADD_ON_MAX_RATIO` | 0.50 | 加单金额上限比例 |
| `ANCHOR_MIN_AMOUNT` | 20.0 | 锚点订单最低金额阈值（低于则不触发R2拆分） |
| `T_REOPEN_MIN_REGULAR` | 30 | 普通桌翻台间隔（分钟） |
| `T_REOPEN_MIN_PRIVATE` | 120 | 包间翻台间隔（分钟） |
| `SMALL_RATIO` | 0.30 | 小单收入比例阈值 |
| `SMALL_LINE_CNT` | 2 | 小单商品行数阈值 |
| `SCORE_THRESHOLD` | 60 | 弱规则打分门槛 |
| `SMALL_ORDER_BONUS_LOW` | 55 | 40元以下小单加分 |
| `SMALL_ORDER_BONUS_MID` | 30 | 40-60元小单加分 |
| `SMALL_ORDER_BONUS_HIGH` | 15 | 60元以上小单加分 |
| `DINNER_MERGE_START_HOUR` | 17 | 晚餐时段开始（时） |
| `DINNER_MERGE_END_HOUR` | 23 | 晚餐时段结束（时） |

## 七、异常检测规则

位于 `merge_order_zhuofang.py` 的 `detect_anomalies()` 函数。按优先级从上到下检查，每个团体最多命中一条规则。

| 规则 | 条件 | 标注文案 |
|------|------|----------|
| 1a | 人数 ≥ 5 且商品数 ≤ 3 | `X人仅Y件商品，请确认就餐人数` |
| 1b | 人数 ≥ 3 且商品数 ≤ 2 | `X人仅Y件商品，疑似人数多录` |
| 1c | 人数 ≥ 2 且商品数 = 1 | `X人仅1件商品，请关注` |
| 2a | 人数 ≥ 2 且人均 < 10 元 | `人均仅¥X（X人），疑似人数多录或免单` |
| 2b | 人数 ≥ 3 且人均 < 20 元 | `人均仅¥X（X人）` |
| 2c | 人数 ≥ 4 且人均 < 30 元 | `人均仅¥X（X人），偏低` |
| 3 | 同桌台、间隔 ≤ 90 分钟、前一团体 ≥ 4 件、后一团体 ≤ 2 件且 ≥ 3 人 | `疑似与前一桌（HH:MM-HH:MM）同一批客人` |

**设计原则**：异常检测仅基于"消费团体"整体指标（团体人数、团体商品总数、人均消费），不对已合并的子订单做逐单判断——合并后的子订单是完整消费的一部分，单独看某个子订单的人数/商品数没有意义。

### 7.1 门店差异化参数

保利店位于写字楼下，人均消费偏低，异常检测阈值与万荷店不同：

| 参数 | 万荷店 | 保利店 |
|------|--------|--------|
| 规则 2a（人均过低疑似免单） | < ¥10 | < ¥15 |
| 规则 2b（人均偏低） | < ¥20 | < ¥15 |
| 规则 2c（人均偏低） | < ¥30 | < ¥15 |
| 订单索引红色预警线 | 人均 < ¥100 | 人均 < ¥40 |
| 重点菜品 | 14 道（见 8.1 第六章） | 2 道（川南鱼香肉丝、香菜回锅茄子） |
| PDF 第七章 | 重点新品销售统计（10 款） | 午晚热销统计（午市/晚市各 Top 6） |
| 桌台分类关键词 | 包间 / 大厅 / 户外 | 包房 / 沙发 / 户外 |

## 八、PDF 报告结构

### 8.1 订单+桌访合并报告（`merge_order_zhuofang.py` 生成）

纵向 A3，包含以下章节：

| 章节 | 内容 |
|------|------|
| 标题页 | 门店、营业日期、生成时间、团体数、桌访覆盖率 |
| **一、数据总览** | 整体营业额/百分比/人数/人均 → 按区域（包间/大厅/户外）→ 午市（<16:00）→ 晚市（≥16:00），各时段再细分区域 → 会员与非会员（全天整体对比）；百分比列示各子项占上级维度比例；附人数修正说明 |
| **二、订单数量明细** | 第一表按两步公式展示：原始订单数 → 扣除外卖/非堂食/免单(零收入)/被合并 → 合并后团体数 → 扣除零食/打包/零散/吧台 → 统计团体数；第二表按消费团体展示乐享卡/纯储值卡、折扣会员卡、其他会员、普通非会员的团体数及占比。PDF 与 Markdown 必须同步输出。 |
| **三、客单价区间分布** | 300+、200-300、150-200、100-150、<100 各区间计数与占比；300+和200-300区间列出开单人，格式为「姓名(开单数)」，按开单数从高到低排序 |
| **四、开单人统计** | 开单数量、开单金额两列，按开单数量降序排列，含合计行；"顾客/系统"标注"扫码点餐" |
| **五、桌访人统计** | 桌访服务员按反馈数量降序排列，含合计行（一人可能服务多桌，合计可能大于团体数） |
| **六、重点菜品销售统计** | 14道重点菜品销售份数，按数量降序（古法干烧鱼合并江团+鲈鱼），深藏青表头白字 |
| **七、重点新品销售统计** | 10款重点新品销售份数，按数量降序，深藏青表头白字 |
| **八、疑似异常汇总** | 所有异常团体的桌台、人数、商品数、人均、异常说明（深藏青表头白字）；无异常时显示「本报告期无疑似异常消费团体」 |
| **九、订单索引** | 标题下方醒目标注桌访覆盖率；若有未匹配桌访记录，以独立表格列出完整信息（9列，含支付金额、下单时间、会员状态、语音转录全文） |
| **订单索引表** | 按桌台→日期→下单时间排序的完整索引（16列，含日期列），翻页自动重复表头 |
| **逐团体详情** | 每团体一页：基本信息表、异常提示（红色）、子订单列表、商品明细（仅展示菜品收入>0的商品）、桌访内容（三选二匹配的记录橙色标注匹配方式） |

会员及卡消费类型按消费团体判定，四类互斥，优先级如下：

1. 支付方式含「会员卡」，且团体全部子订单的 `总优惠金额` 合计为 0：乐享卡/纯储值卡消费（无折扣会员卡）。
2. 支付方式含「会员卡」，且团体全部子订单的 `总优惠金额` 合计非 0：折扣会员卡消费（有折扣会员卡）。
3. 支付方式不含「会员卡」，但任一子订单存在有效会员姓名或会员手机号：其他会员消费（非卡支付但关联会员）。
4. 其余：普通非会员消费（无会员信息）。

四类团体数合计必须等于统计消费团体数，占比以统计消费团体数为分母；不闭合时报告生成失败。

字段口径说明：`总优惠金额`（当前 POS 中与 `订单优惠` 一致）反映订单消费金额相对原价的折扣；`支付优惠` 反映支付渠道优惠。会员卡是否发生消费折扣必须使用 `总优惠金额`，禁止误用 `支付优惠`。

### 8.2 历史纯订单客单价分析报告模块（`pdf_generator.py`）

注：独立纯订单分析 CLI 入口 `jin-gu-cang-order-analysis/scripts/run_analysis.py` 已废弃并从仓库删除；以下模块为历史报告生成模块，不再作为独立工具入口暴露。纯订单分析能力由 `merge_order_zhuofang.py` 的无桌访模式覆盖。

纵向 A4，包含：

- 数据总览、订单数量明细、客单价区间分布
- 重点菜品销售统计、重点新品销售统计
- 客单价最高 3 个订单（逐单详情）
- 客单价最低 3 个订单（逐单详情）
- 经营团队分析建议

### 8.3 历史全量订单列表模块（`pdf_generator_complete.py`）

纵向 A4，包含：

- 完整索引表（含人均排名）
- 所有团体的逐页详情（基本信息 + 商品明细）

### 8.4 周期对比分析报告（`pdf_report.py` + `word_report.py` 生成）

纵向 A4 PDF + Word（.docx），并附加“周几半年趋势”Excel（.xlsx）与会议展示 PDF（.pdf）。

PDF + Word 包含五个章节：

| 章节 | 内容 |
|------|------|
| 标题页 | 门店、本期周期、环比周期及日期范围、同比周期及日期范围 |
| **一、经营数据对比** | 整体/分区/午晚市营业额、人数、人均，每项含本期值、环比变化及变化率、同比变化及变化率；绿色为增长、红色为下降 |
| **二、酒水饮料甜品销售排行** | 本期销量降序，含商品分类列；末尾带规格描述（杯/小瓶/大瓶等）的同名商品归组；排除菜品收入 ≤ 0 的免单/测试商品 |
| **三、重点菜品对比** | 重点菜品本期销量 vs 环比/同比销量，含变化量及变化率；本期销量后展示本期总团数与点购率，点购率 = 本期销量 / 本期统计消费团体数，用于粗略判断每道重点菜品相对到店桌数的点购强度 |
| **四、商品中类销售额分布** | 按商品中类汇总 POS 店内商品归因销售额，含本期占比、环比/同比金额变化及变化率；附合计行；套餐父项排除，套餐子项归入实际品类 |
| **五、客单价区间对比** | 300+/200-300/150-200/100-150/<100 五档，每档本期单数与占比 vs 环比/同比单数变化及变化率 |
| 附录 | 数据完整性说明（环比或同比数据缺失时告警） |

PDF 所有表格设置 `repeatRows` 跨页自动重复表头；Word 表格使用 `tblHeader` XML 元素实现同等效果。

注：经营数据第一节的整体营业额用于和后台收入闭合，包含 POS 店内订单收入与第三方平台外卖已完成订单收入。第四节商品中类/大类销售额只做 POS 店内商品归因，排除套餐父项、保留套餐子项，合计应与 POS 店内订单收入闭合；第三方平台外卖若无商品级明细，不纳入商品分类销售额。

附加 `周几半年趋势_YYYYMMDD_YYYYMMDD_门店.xlsx` 与 `.pdf`：以最新周期所在周为终点，向前取 26 个自然周，按周一至周日生成 7 组表格。每组列出同一星期几连续 26 周的堂食分桌有效消费团体营业额、团体数、总人数、整体人均，以及大厅/包房/院子人数和人均，并生成营业额、团体数/人数、分区域人均曲线图。营业额不使用整体营业额，不纳入自取外卖、吧台及零食购买团体、第三方平台外卖；包房对应库内 `包间`，院子对应库内 `户外`。PDF 采用平台外卖报告的深蓝表头、浅灰隔行表格和折线图风格，优先服务会议投屏和讲解。

### 8.5 平台外卖统计报告（`平台外卖统计` 生成）

Excel + PDF + Markdown，Excel 为主报告，PDF/Markdown 为摘要。平台外卖报告不展示收货人姓名、手机号、送餐地址等隐私字段。

| Sheet / 章节 | 内容 |
|------|------|
| **一、数据总览** | 报告周期、门店数、有效外卖订单数、退单数、订单收入、顾客实付、外卖客单价、抽佣率、订单支出率、部分退款 |
| **二、门店对比** | 万荷店/保利店/湾里店横向对比：订单数、订单收入、顾客实付、客单价、抽佣、支出、退单 |
| **三、每日趋势** | 按营业日展示订单数、订单收入、顾客实付、客单价、抽佣率、订单支出率 |
| **四、平台来源分析** | 美团外卖、淘宝闪购餐饮外卖等平台的订单数、收入、客单价、抽佣率、退单数 |
| **五、时段分布** | 按下单小时统计订单数和收入；同时给出午市（<16:00）/晚市（≥16:00）汇总 |
| **六、履约效率** | 平均接单耗时、平均送达时长、P50/P90 送达时长、超时关注单数（默认下单到送达 > 45 分钟） |
| **七、退单与异常** | 已退单、待配送、部分退款、订单金额为 0、时间字段缺失等记录汇总 |
| **八、脱敏明细** | 可选输出有效外卖明细；隐私字段不展示，仅保留平台、订单号、时间、状态、金额字段 |

PDF 摘要报告建议只包含前七个章节，不输出逐单明细。Markdown 摘要用于快速复制到经营复盘或内部沟通。

### 8.6 GUI 操作台结果页

GUI 操作台不生成新的经营分析报告格式，但需要为四个业务功能提供统一的结果页。

| 区域 | 内容 |
|------|------|
| 任务摘要 | 功能名称、门店、日期范围、输入文件数、输出目录、数据库路径、开始/结束时间、运行耗时、任务状态 |
| 预校验摘要 | Error/Warning/Info 列表；运行后仍保留本次 Warning，便于复盘 |
| 日志面板 | 底层 CLI/模块输出、异常堆栈摘要、生成文件记录；支持复制文本 |
| 输出文件列表 | PDF、MD、Excel、Word、SQLite 等输出，显示文件名、类型、大小、生成时间 |
| 快捷动作 | 打开报告、打开输出目录、复制等价 CLI 命令、复制错误信息 |

GUI 不直接内嵌 PDF/Word/Excel 渲染器；优先调用系统默认应用打开文件。若文件不存在、被占用或生成失败，结果页需明确显示失败原因。

## 九、中文字体处理

PDF 生成依赖系统级中文字体。各模块在启动时自动搜索以下路径：

**macOS 优先级**：
1. `/System/Library/Fonts/STHeiti Medium.ttc`
2. `/System/Library/Fonts/STHeiti Light.ttc`
3. `/System/Library/Fonts/PingFang.ttc`

**Linux 备选**：
1. `/usr/share/fonts/truetype/wqy/wqy-microhei.ttc`
2. `/usr/share/fonts/truetype/wqy/wqy-zenhei.ttc`

所有字体均回退失败最终 fallback 到 Helvetica（中文将无法显示）。如在非 macOS 环境部署，请确保系统中存在上述任一字体，或修改各 `register_chinese_font()` 函数中的字体路径。

## 十、模块依赖关系

```
merge_order_zhuofang.py（订单桌访合并）
  ├── 依赖 order_merger_skill（通过 sys.path 动态引入）
  │     ├── data_loader.py
  │     ├── order_merger.py → config.py
  │     ├── aggregator.py
  │     └── item_report_helpers.py
  └── 不依赖 zhuofang_processor.py（两者独立）

main.py（长期订单分析）
  ├── 依赖 order_merger_skill（同上）
  ├── db_manager.py → SQLite 持久化与增量检测
  ├── multi_file_loader.py → 多文件加载去重
  ├── daily_stats.py → 每日统计计算
  └── excel_writer.py → 4-Sheet Excel 输出

main.py（周期对比分析）
  ├── 依赖 order_merger_skill（同上）
  ├── db_manager.py（复用长期订单分析的） → 历史数据读取
  ├── period_validator.py → 周期完整性校验
  ├── comparator.py → 环比/同比计算
  ├── pdf_report.py → PDF 报告
  └── word_report.py → Word 报告

main.py（平台外卖统计）
  ├── 不参与 order_merger_skill 的堂食合并算法
  ├── takeaway_loader.py → 外卖 Excel 表头定位、清洗、脱敏
  ├── takeaway_stats.py → 门店/日期/平台/时段/履约/退单统计
  ├── excel_writer.py → 多 Sheet Excel 报告
  ├── pdf_report.py → PDF 摘要报告
  └── db_manager.py（复用长期订单分析的 SQLite 连接或抽出公共 DB 层） → 外卖统计表写入

GUI控制台/main.py（统一图形入口）
  ├── validators.py → 文件扫描、类型识别、预校验分级
  ├── app_state.py → 最近目录、输出目录、数据库路径、门店偏好
  ├── task_runner.py → 后台运行四个业务功能，生成等价 CLI 参数
  ├── output_registry.py → 收集输出文件，提供打开文件/打开目录
  └── views/*.py → 四个功能页的界面状态与参数收集
```

**关键设计**：堂食相关工具通过相对路径 `../每日订单分析/order_merger_skill` 导入核心包，不依赖 pip install；平台外卖统计不使用堂食合并算法；GUI 操作台只编排现有四个业务功能，不复制统计规则。运行时需保持上述目录结构不变。

## 十一、SQLite 数据库结构

长期订单分析、周期对比分析、平台外卖统计共用 SQLite 数据库。堂食相关表由 `长期订单分析/db_manager.py` 的 `DatabaseManager` 类创建和维护；平台外卖统计需在同一数据库中新增外卖相关表。堂食相关 7 张表：

| 表名 | 用途 | 去重键 |
|------|------|--------|
| `orders` | 原始订单（整行 JSON 序列化存储） | 门店营业日快照替换：重导完整 POS 范围时覆盖当前订单，并删除该范围内已不存在的旧订单 |
| `items` | 商品明细（整行 JSON 序列化存储） | 随门店营业日快照整体替换：先删除范围内旧订单商品，再写入当前 POS 行级明细 |
| `groups` | 消费团体聚合结果 | 随门店营业日快照整体替换；订单改桌或整桌撤销时必须清除旧团体；UNIQUE `(group_date, first_order_id, table_name)` |
| `daily_overview` | 每日经营数据总览（整体/分区/午晚市/会员） | PRIMARY KEY `(date, category, sub_category)` |
| `daily_order_counts` | 每日订单数量明细（pipeline 各阶段计数） | `date` PRIMARY KEY |
| `daily_buckets` | 每日客单价区间分布（5 档） | PRIMARY KEY `(date, bucket)` |
| `daily_opener_stats` | 每日开单人统计 | PRIMARY KEY `(date, opener_name)` |

`orders`、`items` 与 `groups` 共同组成 POS 快照。跨文件、跨周/月重复导入完整 POS 日期范围时，以订单内容中的“门店 + 营业日”为替换边界：先识别该范围内旧订单和旧团体，再删除当前文件已不存在的订单、全部相关商品行及旧团体，最后写入当前订单、POS 行级商品明细和重新聚合的团体。双店同日可能存在相同桌台名，删除范围必须根据订单内容中的门店归属判定，禁止仅按 `group_date + table_name` 跨店删除。

快照替换必须在单个 SQLite 事务中完成；任一订单、商品、团体或带门店维度的 `daily_overview`、`daily_order_counts`、`daily_buckets` 写入失败时必须整体回滚并向上抛错。写入前先删除当前门店营业日范围内旧日汇总，防止整桌撤销或分类归零后旧分类行残留。`daily_opener_stats` 当前没有 `store_name`，保持原兼容写入；如需补门店维度必须作为独立 Schema 迁移另行确认。团体写入禁止 `except Exception: continue` 或 `INSERT OR IGNORE` 静默掩盖数据错误。`items` 表不得以 `(订单号, 商品编码, 商品名称)` 或类似商品身份去重，因为同一订单内同一商品可能多次加单或拆行，必须保留 POS 当前快照的每一条商品行。`source_file` 是来源证据，常规入库路径不得批量重标商品行来源。

历史数据库可能曾保留旧的商品级唯一索引或 `source_file + item_line_key` 行级唯一索引。新建数据库以 `db_manager.py` 当前建表语句为准；旧库如需正式清理，应按订单快照从原始 POS 文件重建受影响订单商品明细，不得粗暴按商品名去重。

所有 `daily_*` 表通过 `INSERT OR REPLACE` 写入，支持重复运行自动覆盖更新。

`get_items_for_period()` 从 groups 表的 `order_ids` JSON 数组中解析所有被合并订单的 ID，确保子订单的商品也被纳入统计，而非仅查 `first_order_id`。

平台外卖统计新增 4 张表：

| 表名 | 用途 | 去重键 |
|------|------|--------|
| `takeaway_orders` | 平台外卖订单脱敏明细（整行 JSON 序列化存储，不含收货人姓名/手机号/地址） | UNIQUE `(store_name, order_source, takeaway_order_id)` |
| `takeaway_daily_overview` | 每日外卖经营总览（按门店、日期） | PRIMARY KEY `(date, store_name)` |
| `takeaway_platform_stats` | 每日平台来源统计（美团/淘宝闪购等） | PRIMARY KEY `(date, store_name, order_source)` |
| `takeaway_hourly_stats` | 每日下单小时统计 | PRIMARY KEY `(date, store_name, hour)` |

外卖表与堂食 `orders/groups/daily_*` 表相互独立，不通过 `收银订单号` 与堂食订单合并。`收银订单号` 仅作为人工追溯字段保存在脱敏后的外卖原始数据中。

## 十二、添加到新环境的部署清单

1. `pip install pandas openpyxl reportlab python-docx`
2. 如需运行 GUI：`pip install PySide6`，或在 `GUI控制台/requirements.txt` 中统一安装
3. 确保系统有中文字体（macOS 自带 STHeiti/PingFang，Linux 需安装 wqy-microhei）
4. 保持目录结构不变（`merge_order_zhuofang.py` 依赖 `../每日订单分析/order_merger_skill/`）
5. 准备数据文件（POS 订单 Excel、桌访 CSV、平台外卖订单 Excel）
6. 推荐执行第三章中的 GUI 命令；开发、自动化或排查问题时执行对应 CLI 命令

## 十三、已知限制与注意事项

1. **日期提取**：从 Excel 实际数据的 `下单时间` 列提取起止日期（最小值和最大值），不再依赖文件名；支持单日、多日、任意时间段
2. **门店推断**：通过订单号与 CSV 记录的交集自动判断门店，如果数据跨门店可能推断不准确，建议显式传 `--store`
3. **免单处理**：`订单收入 <= 0` 的订单会被排除，不计入任何统计
4. **外点自取**：桌台字段含"外点自取"的订单会被排除（非堂食场景）
5. **吧台订单**：桌台字段含"吧台"的订单会被排除，不计入统计范围（吧台为零食/饮品购买，非正餐消费）
6. **会员分类**：一个消费团体中任一订单的"会员姓名"非空（非"-"），该团体即归为会员；仅用于"一、数据总览"中会员与非会员人均对比
7. **桌面系统路径**：字体路径硬编码了 macOS 和 Linux 路径，Windows 需自行修改
8. **合并算法局限**：同一桌台短时间内翻台两次（前客离店、后客立即入座），算法可能误判为同一拨人，目前通过 R2（金额上限）和 R3（结账后间隔）来降低误判率
9. **桌台号命名差异**：CSV 桌访的桌台号（如 "B07"）与 POS 订单的桌台名（如 "大厅B区B07"）可能存在前缀差异，金额+桌台匹配已通过子串模糊匹配处理此问题，但极端情况下（如两个不同区域有同名桌台号）可能误匹配
10. **订单快照唯一性**：orders 与 items 表以订单快照为唯一性边界；同一订单重导时覆盖该订单原始订单 JSON，并整体替换该订单商品明细，当前快照内部保留 POS 行级明细，避免周/月文件或历史包/补充包重复导入导致商品销售额虚增或订单原始数据滞留旧快照
11. **酒水饮料归组**：周期对比分析的酒水饮料甜品排行中，同名商品的不同规格（杯/小瓶/大瓶/扎/壶/盅）按核心品名归组显示
12. **纯订单独立入口已废弃**：`每日订单分析/jin-gu-cang-order-analysis/` 与 `run_analysis.py` 已从仓库删除；纯订单分析不再作为独立工具维护，由主工具无桌访模式覆盖
13. **长期/周期订单数量明细的过滤项统计待重构**：当前 `aggregate_groups()` 内部已先调用 `filter_groups()`，长期订单分析和周期对比分析后续再按日期过滤时已看不到零食、打包、零散小单、吧台等被过滤团体，导致 `daily_order_counts` 中相关过滤列可能长期为 0。目标改造方向：为 `aggregate_groups()` 增加不立即过滤的模式（如 `skip_filter=True`），聚合后先为每个团体标记 `_filter_status`，保留被过滤团体传入 `compute_all_daily_stats()`，再按状态统计。已修复：长期订单分析多文件入库的 `原始订单数`、`外卖订单数`、`非堂食订单数` 按门店分别传入，不再把双店合计重复写入每个门店行。
14. **周期对比保利店分支存在无效导入**：`周期对比分析/comparator.py` 中 `from order_merger_skill import config` 未被使用，且当前 `sys.path` 指向 `order_merger_skill` 目录本身，运行保利店分支时可能触发 `ModuleNotFoundError`；应直接删除该无效导入
15. **合并算法代码注释需同步**：`order_merger.py` 顶部注释应补充 R3.5 包间晚餐纯酒水强合并，并将 R1 描述从"包间小单例外"修正为"小单例外适用于所有桌台"
16. **外卖无商品明细排行**：当前平台外卖订单明细只有订单级字段，没有逐商品明细，因此外卖统计不做菜品销量排行；若未来导出平台商品明细，需另行扩展输入格式和报表章节
17. **外卖财务到账口径待确认**：默认以 `订单收入` 作为外卖营业额，以 `顾客实付`、`订单支出`、`外卖抽佣` 并列展示，不默认计算最终到账金额；如需财务结算口径，需先确认平台字段含义
18. **外卖隐私字段限制**：收货人姓名、手机号、送餐地址不得进入汇总报告和长期统计数据库；脱敏明细也只能展示非隐私字段或脱敏值
19. **GUI 预校验不是最终业务校验**：GUI 只在运行前发现结构性错误和明显风险；最终统计口径、过滤逻辑、异常检测仍以各业务 pipeline 为准
20. **GUI 并发写库限制**：同一时间只允许一个任务写入同一个 SQLite 数据库，避免长期订单、周期对比、外卖统计同时写库造成锁冲突或结果覆盖
21. **GUI 不编辑原始数据**：拖入文件只读处理；任何字段修正、脱敏、过滤结果只能出现在输出报告或数据库中，不得回写原 Excel/CSV

---

## 十四、版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 3.24 | 2026-07-27 | POS 快照替换边界扩展为门店营业日完整范围；订单、商品、团体在同一事务中原子替换，覆盖订单改桌、整桌撤销和写入失败整体回滚 |
| 3.23 | 2026-07-08 | 周期对比第三节重点菜品对比新增“总团数”和“点购率”列，点购率按本期销量 / 本期统计消费团体数计算 |
| 3.22 | 2026-07-08 | 周期对比第一节经营数据对比补齐包间/大厅/户外三项区域人均，使分区营业额、人数、人均成组展示 |
| 3.21 | 2026-07-07 | 周期对比新增“周几半年趋势”Excel/PDF：按周一至周日拆 7 组表格，连续观察近 26 周堂食分桌营业额、团体数、人数与分区域人均，并生成曲线图 |
| 3.20 | 2026-07-01 | POS 入库改为订单快照替换模型：重导同一订单时同步覆盖 `orders` 原始 JSON 并替换 `items` 商品明细；禁止常规路径批量重标 `items.source_file`；周期对比报告生成前新增 POS 商品归因收入与 POS 店内订单收入闭合校验 |
| 3.19 | 2026-06-30 | 周期对比第四节与饮品/酒水报告统一改为商品归因口径：排除套餐父项、保留套餐子项，避免套餐父子重复统计；饮品额占比分母改为整体营业额，并在摘要中展示 POS 店内订单收入、第三方平台外卖收入与整体营业额闭环 |
| 3.18 | 2026-06-29 | 修复长期订单分析多文件入库 `daily_order_counts` 预合并计数字段按双店合计重复写入分店行的问题；判店规则明确为读取 POS 内容，文件名只作一致性校验；万荷重点菜品对比清单以「百合蜜枣无花果排骨汤」替换「五指毛桃白芸豆猪肚三年老鸡汤(盅)」 |
| 3.17 | 2026-06-24 | 饮品/酒水订单统计：新增周/月同比环比（本期 Excel + 主库对比期）；摘要三期原始数据 + 【本周说明】正文；分品类/维度「本周简表」与「三行对比表」分章；含对比时 A4 横置 |
| 3.16 | 2026-06-22 | 新增饮品/酒水订单统计（万荷九类中类、消费团体口径、A4 纵置 PDF）；新增桌访语料转换文档入 PRD；门店识别以 Excel「门店名称」为准；SQLite 唯一主库路径固化 |
| 3.15 | 2026-05-27 | 新增统一 GUI 操作台设计：补充 GUI 目录结构、推荐启动入口、文件拖入与预校验规则、GUI 编排 pipeline、结果页、模块依赖、部署依赖与并发写库限制 |
| 3.14 | 2026-05-27 | 新增平台外卖统计功能设计：补充外卖 Excel 输入格式、独立 pipeline、统计口径、报告结构、SQLite 外卖表设计、隐私字段处理与外卖财务口径限制 |
| 3.13 | 2026-05-27 | 文档同步：删除已废弃纯订单独立入口与 `jin-gu-cang-order-analysis/` 目录说明；主工具补充无桌访纯订单模式；修正桌访独立工具章节编号为 3.7；补充长期/周期过滤项统计偏差、保利店周期对比无效导入、历史 SQLite 旧约束、order_merger.py 注释同步等待处理事项 |
| 3.12 | 2026-05-27 | 文档完善：PRD 新增 SQLite 数据库结构章节、门店差异化参数章节、周期对比分析报告结构章节；CLAUDE.md 新增 SQLite Schema、门店差异化参数完整清单、酒水饮料归组说明；模块依赖关系扩展覆盖全部三个工具 |
| 3.11 | 2026-05-22 | 周期对比分析新增 Word(.docx) 输出；新增"酒水饮料甜品销售排行"章节（含商品分类列、同名归组）；商品中类销售额分布改为金额对比含合计行；所有统计表格跨页自动重复表头；PDF 报告标题区增加环比/同比周期日期范围 |
| 3.10 | 2026-05-16 | 新增周期对比分析工具（周/月模式，环比+同比）；新增长期订单分析工具（4-Sheet Excel + SQLite）；订单合并时间窗口从 1h 调整为 2h；R1 小单例外从仅包间扩展至所有桌台；保利店差异化参数（异常检测 ¥15、红色预警 ¥40、午晚热销 Top 6）；数据闭合校验；items 表去重修复 |
| 3.9 | 2026-05-09 | 桌访匹配优化：有订单号但 POS 中不存在的记录不再静默丢弃，转入三选二模糊匹配（桌台+金额+时间） |
| 3.8 | 2026-05-04 | 当前版本 |
